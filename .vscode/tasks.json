{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "watch",
            "command": "chrome http://localhost:3000/ & sass --watch src/css/style.scss dist/style.css & tsc -w & browser-sync . -w  --no-notify && bg ",
            "args": [],
            "type": "shell",
            "problemMatcher": []
        }
    ]
}
